# Core Rules (Mandatory)

1. **vvv before nnn** — verification gates planning.
2. **Read actual code** — no assumptions; evidence-based.
3. **Human approves deploy & destructive ops** — `decided_by: human`.
4. **Audit chain append-only** — never edit `.ai/audit/events.ndjson`.
5. **Policy & schema are human-owned** — never auto-edit `.ai/policies/`, `.ai/schemas/`.
6. **AI proposes; Verifier/Policy/Human decides** — never self-certify completion.

See full Trinity Constitution in upstream `docs/constitution/`.
